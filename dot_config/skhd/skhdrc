# Config for skhd keymaps
# Disable macOS cmd+h hide as it conflicts with yabai mapping
cmd - h: :
# APP MAPPINGS
# ctrl - return : AX_APPLICATION=1 kitty # open -a "kitty"
ctrl - return : open -a "Ghostty"

# "Show desktop" (actually just change all displays to first empty space)
alt - d : sh -c '"$XDG_CONFIG_HOME"/skhd/util/focus_empty_space.sh'

# change window focus within space
ctrl + alt - j : yabai -m window --focus south
ctrl + alt - k : yabai -m window --focus north
ctrl + alt - h : yabai -m window --focus west
ctrl + alt - l : yabai -m window --focus east
ctrl + alt - p : yabai -m window --focus recent
#change focus between external displays (left and right)
alt - w: yabai -m display --focus west
alt - e: yabai -m display --focus east
# rotate layout clockwise
shift + alt - r : yabai -m space --rotate 270

# flip along y-axis
shift + alt - y : yabai -m space --mirror y-axis

# flip along x-axis
shift + alt - x : yabai -m space --mirror x-axis

# toggle window float
shift + alt - t : yabai -m window --toggle float --grid 4:4:1:1:2:2
# maximize a window
shift + alt - m : yabai -m window --toggle zoom-fullscreen

# balance out tree of windows (resize to occupy same area)
shift + alt - e : yabai -m space --balance
# swap windows
# shift + alt - j : yabai -m window --swap south
# shift + alt - k : yabai -m window --swap north
# shift + alt - h : yabai -m window --swap west
# shift + alt - l : yabai -m window --swap east
# move window and split
shift + alt - j : yabai -m window --warp south
shift + alt - k : yabai -m window --warp north
shift + alt - h : yabai -m window --warp west
shift + alt - l : yabai -m window --warp east
# move window to display left and right
shift + alt - w : yabai -m window --display west; yabai -m display --focus west;
shift + alt - e : yabai -m window --display east; yabai -m display --focus east;
#move window to prev and next space
shift + alt - p : yabai -m window --space prev;
shift + alt - n : yabai -m window --space next;

# move window to space #
shift + alt - 1 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 1; yabai -m window --focus $wid'
shift + alt - 2 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 2; yabai -m window --focus $wid'
shift + alt - 3 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 3; yabai -m window --focus $wid'
shift + alt - 4 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 4; yabai -m window --focus $wid'
shift + alt - 5 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 5; yabai -m window --focus $wid'
shift + alt - 6 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 6; yabai -m window --focus $wid'
shift + alt - 7 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 7; yabai -m window --focus $wid'
shift + alt - 8 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 8; yabai -m window --focus $wid'
shift + alt - 9 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 9; yabai -m window --focus $wid'
shift + alt - 0 : sh -c 'wid=$(yabai -m query --windows --window | jq -r ".id"); yabai -m window --space 10; yabai -m window --focus $wid'

# Change focus between spaces
# FIXME: focusing a space will only work if we are focused on a space in a display that is not the display with the space we are trying to switch focus to
# The below works to focus spaces on our current display, but doesn't work across displays:
# alt - 1 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==1) | .windows[0]")'
# alt - 2 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==2) | .windows[0]")'
# alt - 3 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==3) | .windows[0]")'
# alt - 4 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==4) | .windows[0]")'
# alt - 5 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==5) | .windows[0]")'
# alt - 6 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==6) | .windows[0]")'
# alt - 7 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==7) | .windows[0]")'
# alt - 8 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==8) | .windows[0]")'
# alt - 9 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==9) | .windows[0]")'
# alt - 0 : sh -c 'yabai -m window --focus $(yabai -m query --spaces | jq -r ".[] | select(.index==10) | .windows[0]")'
alt - 1 : yabai -m space --focus 1
alt - 2 : yabai -m space --focus 2
alt - 3 : yabai -m space --focus 3
alt - 4 : yabai -m space --focus 4
alt - 5 : yabai -m space --focus 5
alt - 6 : yabai -m space --focus 6
alt - 7 : yabai -m space --focus 7
alt - 8 : yabai -m space --focus 8
alt - 9 : yabai -m space --focus 9
alt - 0 : yabai -m space --focus 10
alt - 0x1E : yabai -m space --focus next
alt - 0x21 : yabai -m space --focus prev

# # create a new space and follow focus
# alt - n : yabai -m space --create && \
#                   index="$(yabai -m query --displays --display | jq '.spaces[-1]')" && \
#                   yabai -m space --focus "${index}"
#
# # create a new space, move window and follow focus
# alt - n : yabai -m space --create && \
#                   index="$(yabai -m query --displays --display | jq '.spaces[-1]')" && \
#                   yabai -m window --space "${index}" && \
#                   yabai -m space --focus "${index}"

# change layout of desktop
alt - b : yabai -m space --layout bsp
alt - f : yabai -m space --layout float
alt - s : yabai -m space --layout stack
# Toggle current space layout between stack and bsp
alt - t : sh -c '[ "$(yabai -m query --spaces --space | jq -r ".type")" = "stack" ] && yabai -m space --layout bsp || yabai -m space --layout stack'

# toggle window native fullscreen
shift + alt - f : yabai -m window --toggle native-fullscreen

# delete focused space
shift + alt - q : yabai -m space --destroy

# stop/start/restart yabai
ctrl + alt - q : yabai --stop-service
ctrl + alt - s : yabai --start-service
ctrl + alt - r : yabai --restart-service
alt - r : skhd --restart-service && yabai --restart-service && sleep 1 && yabai -m rule --apply
alt - a : yabai -m rule --apply
