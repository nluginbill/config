# PATH
# set -gx PATH $PATH $HOME/.local/share/uv/python
# set -gx PATH $PATH $PYENV_ROOT/shims

# Disable Fish's venv prompt addition
set -gx VIRTUAL_ENV_DISABLE_PROMPT 1

# pyenv config
# set -gx PYENV_ROOT "$HOME/.pyenv"
